/* Курси та уроки рендерять свій власний layout (sidebar/full-screen).
   Цей layout — просто прохідник. */
export default function CourseLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
