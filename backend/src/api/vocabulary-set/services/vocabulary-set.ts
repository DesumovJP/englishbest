import { factories } from '@strapi/strapi';

// Cast — generated types regenerate on next BE boot in prod; keeps
// dev typecheck green until then.
export default factories.createCoreService('api::vocabulary-set.vocabulary-set' as any);
